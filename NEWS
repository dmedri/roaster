CHANGES WITH ...
	* reworked configuration directories;
	* functionality to retrieve mirrors list;
	* code refinements.

CHANGES WITH V0.1

	* checks for latest R release;
	* two build types available (standard, virtualenv);
	* custom fields to setup your build
